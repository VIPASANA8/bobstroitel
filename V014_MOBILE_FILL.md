# v0.14 Mobile Fill

Target tuning viewport: **402 × 874**.

Changes:
- keeps the v0.13 component scale but stretches the table stage vertically for tall phones;
- removes the large empty black zone under the action controls;
- action panel now fills the remaining viewport and pins the auto-action bar near the bottom;
- felt gets proper side breathing room instead of touching the viewport edges;
- positions are spaced vertically without making player cards larger;
- idle visual position badges use BTN/HJ/CO/SB/BB/MP/UTG so layout can be compared with the reference before a hand starts.
