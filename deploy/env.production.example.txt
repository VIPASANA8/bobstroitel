# Poker8 — прод-конфиг. Скопировать в /root/poker8/.env на сервере и заполнить.
# НЕ КОММИТИТЬ (файл .env в .gitignore). Значения <...> заменить реальными.

# Окружение
POKER8_ENV=production

# База данных (хост "postgres" — имя сервиса в compose.server.yaml)
POKER8_DATABASE_URL=postgresql+psycopg://poker8:<STRONG_DB_PASS>@postgres:5432/poker8
POSTGRES_DB=poker8
POSTGRES_USER=poker8
POSTGRES_PASSWORD=<STRONG_DB_PASS>

# Telegram-бот (отдельный токен от autorek!)
POKER8_DEFAULT_BOT_TOKEN=<TELEGRAM_BOT_TOKEN>

# Тенант: hosts ДОЛЖЕН совпадать с реальным доменом за прокси,
# иначе /api/auth -> 404 Unknown tenant host
POKER8_DEFAULT_TENANT=poker8
POKER8_TENANTS_JSON=[{"slug":"poker8","hosts":["donbass.win","www.donbass.win","donbas.win","www.donbas.win"],"name":"Poker8","support_url":"https://t.me/<support>","branding":{},"token_env":"POKER8_DEFAULT_BOT_TOKEN"}]

# Рантайм
POKER8_COORDINATOR_ENABLED=1

# Гостевой доступ ВЫКЛ в проде (в коде всё равно форсится 0 при production)
POKER8_OPEN_ACCESS=0
